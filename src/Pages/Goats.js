import React from 'react';
 
import 'bootstrap/dist/css/bootstrap.min.css';
 
 
export default function  Goats(props) {
  // let [authMode, setAuthMode] = React.useState('Goats');

  // const changeAuthMode = (props) => {
  //   setAuthMode(authMode === '' ? 'Goats' : 'Goats');
  // };

  // const changePage = (page) => {
  //   setAuthMode((authMode = 'Goats'));
  // };

  // const changePage2 = (page) => {
  //   setAuthMode((authMode = 'Goats'));
  // };

  return (
    <div className="Auth-form-container">
      <h3 className="Auth-form-title">Welcome Home Boy From Goats</h3>
    </div>
  );
}
